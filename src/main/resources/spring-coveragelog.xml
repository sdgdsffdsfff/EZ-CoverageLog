<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
          http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context-3.2.xsd
          http://www.springframework.org/schema/data/mongo
          http://www.springframework.org/schema/data/mongo/spring-mongo.xsd
          http://www.springframework.org/schema/aop
         http://www.springframework.org/schema/aop/spring-aop-3.2.xsd">

    <context:component-scan base-package="${component.scan}"/>

    <bean id="propertyHelper"
          class="com.ecfront.easybi.utils.springext.PropertyHelper">
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="locations">
            <list>
                <value>classpath:coveragelog.properties</value>
            </list>
        </property>
    </bean>

    <bean id="coverageLogHock" class="com.ecfront.easybi.coveragelog.CoverageLogHock"/>
    <bean id="methodScanner" class="com.ecfront.easybi.coveragelog.MethodScanner"/>
    <aop:config>
        <aop:aspect ref="coverageLogHock">
            <!--TODO-->
            <aop:around method="around" pointcut="execution (* com.ecfront..*.*(..))"/>
        </aop:aspect>
    </aop:config>

    <mongo:db-factory id="mongoDbFactory"
                      host="${mongo.host}"
                      port="${mongo.port}"
                      dbname="${mongo.dbname}"
                      username="${mongo.username}"
                      password="${mongo.password}"/>

    <bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg ref="mongoDbFactory"/>
        <constructor-arg ref="mongoConverter"/>
        <property name="writeConcern" value="SAFE"/>
    </bean>

    <mongo:mapping-converter id="mongoConverter" base-package="om.ecfront.easybi.coveragelog.document"/>

    <mongo:repositories base-package="com.ecfront.easybi.coveragelog.repositories"/>

</beans>